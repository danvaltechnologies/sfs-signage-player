package com.sundryfoods.player.kiosk

import android.app.admin.DeviceAdminReceiver

/** Lets the box be provisioned as device owner (see Kiosk). Does nothing on its own. */
class SundryDeviceAdmin : DeviceAdminReceiver()
